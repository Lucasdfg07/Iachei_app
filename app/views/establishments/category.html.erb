<section class="establishment_content mt-4">
	<h4><%= @category.name %></h4>

	<hr>

	<%= render 'list_establishments' %>

	<%= link_to 'Voltar', establishments_path(city_id: params[:city_id]), class: 'btn btn-warning mt-4' %>
</section>
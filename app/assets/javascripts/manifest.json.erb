<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "Iachei App",
  "short_name": "Iachei",
  "start_url": "/",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= image_path "/icons/apple-icon-#{dim}.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    },
    {
      "src": "<%= image_path "/icons/android-icon-#{dim}.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#000000",
  "background_color": "#FFFFFF",
  "display": "fullscreen",
  "orientation": "portrait"
}
